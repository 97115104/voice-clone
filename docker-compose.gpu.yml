# GPU overlay — applied automatically on Linux when NVIDIA + Docker GPU runtime detected.
services:
  voice-clone:
    image: voice-clone:gpu
    build:
      target: cuda
    gpus: all
    environment:
      TTS_DEVICE: cuda
